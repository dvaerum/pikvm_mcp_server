# Autonomous roadmap — pointer-prediction pipeline

Living document. Each cron fire reads this, picks the highest-priority **`[ ]` pending** step, executes it end-to-end (including live verification), and updates this file with results before returning. Mark steps `[x]` once verified, `[~]` if partially done with a note, `[!]` if blocked with a description of the blocker.

**North-star goal:** ≥90 % real-launch click rate on icon-sized iPad targets *without* slam-fallback, by combining (a) a cursor-position detector that's accurate enough to skip detection-side fallbacks and (b) a forward pointer-acceleration model that lets `moveToPixel` predict where the cursor will land from an emit sequence.

**Authorization for the cron loop** (user-granted 2026-05-31):
- Make the best long-term decision without checking in — investigate options in detail when unsure.
- Live testing on the iPad is expected and approved.
- Commit completed work to `main` (no force-push, no `--no-verify`, no `--amend`).
- If an unexpected result appears, **investigate the root cause before moving on** — don't paper over it.
- Don't open PRs or push to remotes without user request.

---

## Stage 1 — pointer-acceleration data + model

- [x] **1.1 Smoke trajectory bench** — `benches/bench-collect-trajectory.ts` SHORT_RUN. Verified 2026-05-31: 30 cursor events, 19 sequences, 19.6 s. Output `data/cursor-trajectory-2026-05-30T22-50-03/`.
- [x] **1.2 Full trajectory collection** — Verified 2026-05-31. First `--full` attempt produced only 87 cursor events / 112 emits in 60 s — the script's `--full` mode only multiplies *within* sequences, not across them. Added a `--repeats N` CLI flag (`benches/bench-collect-trajectory.ts` lines 41–63, 332–340) and re-ran `--full --repeats 12`: **1071 cursor events / 1344 emits / 756 sequences in 720 s**. Output `data/cursor-trajectory-2026-05-31T02-43-11/`. All cursor phases are `moved` (no entered/exited gaps). Coalescing ratio = 1071/1344 = 80 % (matches PA38).
- [x] **1.3 Train pointer-accel model** — Verified 2026-05-31. `python ml/train-pointer-accel.py data/cursor-trajectory-2026-05-31T02-43-11 --epochs 200`. **Val MAE x=2.28 px, y=0.67 px** — both clear the 5 px target. Loss converged 133 → 8.4 MSE smoothly, no overfit. Output `ml/pointer-accel-v1.pt`. Full log: `docs/troubleshooting/2026-05-31-pointer-accel-v1-train.md`.
- [x] **1.4 Inspect model honestly** — Verified 2026-05-31. `ml/eval-pointer-accel-v1.py` replays the trainer's SEED=1337 + VAL_FRACTION=0.2 split and aggregates MAE by sequence-label prefix. **All three families pass the 5 px per-axis threshold**:

  | family | n | mae_x | mae_y | p95_x | p95_y | euclid_mae |
  | --- | --: | --: | --: | --: | --: | --: |
  | burst | 129 | 3.71 | 0.51 | 10.64 | 0.82 | 3.92 |
  | direction | 20 | 1.44 | 2.20 | 2.80 | 4.62 | 2.87 |
  | linearity | 119 | 0.87 | 0.59 | 3.78 | 2.53 | 1.24 |

  Overall val: mae_x=2.28, mae_y=0.67, euclid_mae=2.65, euclid_p95=7.79. Burst's p95_x=10.64 with a single ~20 px outlier on `burst:+x:m20:n8:d0` (iPadOS zero-delay coalescing regime) — model is fine for planning open-loop emits but downstream correction loops must still verify, not slam, when zero-delay bursts are involved. Full doc: `docs/troubleshooting/2026-05-31-pointer-accel-v1-eval.md`.
- [x] **1.5 Wire pointer-accel into `src/pikvm/move-to.ts`** — Done 2026-05-31. `ml/export-pointer-accel-onnx.py` exports `ml/pointer-accel-v1.onnx` (opset 17, round-trip max |pt - onnx| = 9.5e-7). `src/pikvm/pointer-accel.ts` lazy-loads the ONNX via `onnxruntime-node` and exposes `buildFeatures` + `predictDisplacement` matching the trainer's 8-feature schema. In `moveToPixel`, when `PIKVM_USE_LEARNED_BALLISTICS=1` the constant `fallback: 1.0` / profile-lookup `pxPerMickey` is replaced by `learnedBallisticsPxPerMickey(...)` which queries the model for a `chunkMag`-mickey steady-state emit per axis, scales logical-px → HDMI-screenshot-px via cached iPad bounds (820×1180 logical, matches trajectory bench manifest), and divides by `chunkMag`. Forward-once "Newton/scale" inversion documented in code. Flag OFF = byte-identical to prior behaviour. 12 new unit tests in `src/pikvm/__tests__/pointer-accel.test.ts` pass; 0 regressions in the move-to suite (the pre-existing forbidSlam / verificationLag failures predate this change). Live A/B left to 1.6 — iPad in use by user, no live run from this fire.
- [!] **1.6 Live A/B `forbidSlamFallback: true` with the learned model** — FAILED 2026-05-31. **Baseline 3/20 HIT (15 %)**, treatment 1/20 HIT (5 %, 16 SKIP, 2 NO-LAUNCH) — Δ −10 pp vs pass criterion +15 pp, **fails by 25 pp**. Residuals balloon from baseline's 38–91 px to 199–767 px. Root cause is a model-mismatch, not a math bug: integration queries `pointer-accel-v1.onnx` with `dt_prev_emit_ms=0`, which is out-of-distribution — trainer's linearity rows all have ≥800 ms gap, so cold-start predictions are garbage across all axes (`+y 20 → +0.6 logical px`, `-y 20 → +18.5 x logical px`). Even with `dt=800`, the model predicts the wrong physical quantity: single-emit-then-settle displacement, not the steady-state px/mickey of the chunked-burst regime move-to actually uses (~4× under). Flag stays opt-in (default off, byte-identical to prior behaviour) — no production impact. Full diagnostic + redesign options in `docs/troubleshooting/2026-05-31-pointer-accel-1.6-fails.md`.
- [!] **1.7 Promote learned-ballistics default** — BLOCKED on 1.6. Three options to unblock listed in the 1.6 diagnostic doc (re-train on chunked-burst regime, use model inside chunk loop with proper history, or drop the steady-state-ratio approach entirely). Pivot Stage 1 budget to Stage 3 — 85 % NO-LAUNCH on a good iPad day means tap-registration has 50+ pp of upside vs ballistics' ≤ 15 pp. **Reopened 2026-06-01**: Stage 3.5 closed and the "85 % NO-LAUNCH" framing was based on the page-2 bug; honest baseline is 85 % HIT, so ballistics-fix headroom is bounded but real (~5 pp on the SKIP fraction). Unblock path: 1.8 + 1.9 + 1.10 below.
- [x] **1.8 Extend `bench-collect-trajectory.ts` with chunked-burst + random-walk sequence types** — 2026-06-01. Added two new sequences to cover the input-space regions move-to actually queries. (a) **`chunkedBurst`**: nested (4 dirs × 4 mags × 4 paces × 3 chain-lengths = 192 sequences per pass) emitting chunks like `chunkMag=20 @ 30 ms` repeated 4-16× — matches `move-to.ts:1439-1440` production constants. (b) **`randomWalk`**: 500-emit chain per pass with seeded-PRNG (mag, dir, inter-emit-delay) drawn from move-to's working distribution; magnitude squared-uniform skewed toward 20-40, delay uniform 10-200 ms, 8-direction sweep. Both sequences added to main loop after linearity/burst/direction. Run wall-time per pass: chunkedBurst ≈ 4.5 min, randomWalk ≈ 50 s. `tsc --noEmit` clean. Will run as soon as 4.1 (currently iPad-bound) finishes — see 1.9.
- [x] **1.9 Collect chunked-burst + random-walk trajectory data** — Verified 2026-06-02. Three attempts at `--repeats 6` died at the same point: ~1100 cursor events into pass-1 chunkedBurst, iPad WS session lost (`resync failed: not connected`). Root cause unproven; most likely iPad-side buffer overflow under sustained ~50 emit/sec inner loop or iPadOS backgrounding iPadCollector. **Fix:** shrunk chunkedBurst matrix from 4 mags × 4 paces × 3 chains × 4 dirs (192 sequences) to 2 × 2 × 2 × 4 (32 sequences) — kept the production move-to regime (mag=20/30, pace=30/50, chain=4/8) and dropped corners that were either out-of-regime (mag=10/50, pace=20/100) or duplicative (chunkedBurst pace=20 is already covered by burst-coalescing). Attempt 4 (post-shrink) ran cleanly through all 3 passes in 487 s with no `not connected` errors. Output `data/cursor-trajectory-2026-06-02T04-46-01/`. Per-family emit counts: linearity 144, burst 168, direction 24, **chunkedBurst 576, randomWalk 1500**, total 2412 emits / 1778 cursor events. All 5 families present.
- [!] **1.10 Train pointer-accel-v2 on combined 1.3 + 1.9 data** — 2026-06-02. Trained on combined 3751 examples (1342 from 1.3 + 2409 from 1.9; per-family: 840 burst / 576 chunkedBurst / 119 direction / 718 linearity / 1498 randomWalk). 200 epochs, MPS. **FAILS the per-family pass criterion.** Best checkpoint at epoch 50 (val_mse=828, climbs steadily after — clear overfit). Overall val MAE: x=7.57, y=7.59 logical px (vs v1's 2.28/0.67). Per-family val MAE (best checkpoint):
  | family | n | mae_x | mae_y | p95_x | p95_y |
  |---|--:|--:|--:|--:|--:|
  | linearity | 142 | 1.29 | 2.86 | 4.17 | 4.84 |
  | direction | 23 | 2.57 | 3.43 | 3.95 | 8.60 |
  | burst | 161 | 6.65 | 1.64 | 19.19 | 4.73 |
  | **chunkedBurst** | **115** | **7.15** | **11.65** | **19.09** | **21.98** |
  | randomWalk | 309 | 8.34 | 7.62 | 31.14 | 26.18 |

  Pass criterion was chunkedBurst MAE < 3 px per axis; we got 7.15 / 11.65 — fails by 4-9 pp. v2 is also worse than v1 on the OLD families it inherited (burst x: 6.65 vs v1's 3.71). Hypothesis (unverified): MLP capacity insufficient for the 3.5× larger and more varied distribution; overfit kicks in early (epoch 50). ONNX exported to `ml/pointer-accel-v2.onnx` (round-trip max |pt - onnx| = 1.9e-6, OK), available if A/B is wanted anyway, but live test against iPad is expected to confirm v2 is worse than v1 — which makes burning ~20 min of iPad time on 1.11 likely wasteful. **Decision: pause the chain.** Iteration options for v3 (next attempt): (a) reduce epochs to ~50 with early-stopping + LR schedule, (b) larger model (more hidden units), (c) re-balance training (~equal weighting per family), (d) collect more chunkedBurst variety (more dirs incl. diagonals, more mag/pace cells now that the WS load issue is understood). 1.9's trajectory data is preserved on disk and reusable for v3.
- [x] **1.11 Re-run 1.6 live A/B with v2 — v2-wider WINS, +55pp HIT rate** (2026-06-02):

  Live A/B: 5 trials × 4 targets × 2 arms = 40 trials total. Production
  defaults (`maxRetries=3`, `maxResidualPx=35`, `requireVerifiedCursor=true`).

  | arm | model | HIT | SKIP | NOLAUNCH | MISS |
  |---|---|---|---|---|---|
  | baseline | `pointer-accel-v1.onnx` | **5/20 (25%)** | 13/20 | 2/20 | 0/20 |
  | treatment | `pointer-accel-v2-wider.onnx` | **16/20 (80%)** | 3/20 | 1/20 | 0/20 |

  Per-target HIT (baseline → treatment): Settings 2/5 → 4/5, Books 2/5 →
  3/5, AppStore 0/5 → 4/5, Files 1/5 → 5/5. +55pp lift on overall HIT
  rate. The +55pp magnitude is well above the documented ±10pp N=20
  noise floor — signal survives the bench-noise gate.

  Surprise vs 1.10 prediction: 1.10's offline gate was misleading.
  v2-wider's chunkedBurst-y MAE was 10.37 px (>>3 px gate) and the
  roadmap predicted "v2 will be worse live". The opposite happened.
  Mechanism: what matters live is whether residual eventually clears the
  `maxResidualPx=35` safety gate after retries — v1 fails that 65% of
  the time (no chunkedBurst training data → residual stays high),
  v2-wider clears it 85%. The 10 px chunkedBurst-y error is *within*
  the 35 px gate. Lesson recorded for future cycles: offline MAE gates
  should be calibrated against the live safety thresholds, not against
  arbitrary "good enough" numbers.

  Honesty notes — limits of this measurement:
  - Single bench, no inter-bench variance estimate. ±10pp confidence
    band per arm; +55pp lift survives that wide.
  - Baseline 25% looks low vs the v11 memory point (93% live HIT);
    consistent with the v11 env-drift re-measurement
    (2026-05-31 = 5%). The treatment 80% under the same drift is the
    relevant comparison.
  - v2-wider promoted to default at `src/pikvm/pointer-accel.ts:61`
    (`resolveDefaultModelPath` returns `pointer-accel-v2-wider.onnx`
    when env-var unset). Test in `pointer-accel.test.ts:168` updated;
    14/14 pass.
  - Raw bench output: `data/click-bench-1.11-baseline/` and
    `data/click-bench-1.11-treatment/` (per-trial JPEGs + reasons).
    Stdout logs at `/tmp/bench-1.11-{baseline,treatment}.log`.
- [!] **1.12 Open-loop move-to prototype — Phase 1 ships, Phase 2 fails compounding-error gate** (2026-06-02):

  Implementation:
  - `src/pikvm/open-loop-planner.ts` (new) — greedy chunk-by-chunk
    planner, cardinal-only, MAX_EMITS=50 termination guard, returns
    `{emits, residualPx, hitMaxEmits, predictorFailed}`.
  - `src/pikvm/__tests__/open-loop-planner.test.ts` (new) — 9 unit
    tests using stub forward models. All pass. Confirms planner
    algorithm correctness (convergence, sign handling, axis dominance,
    feature plumbing through `buildFeatures`, hitMaxEmits + predictor-
    null reporting).
  - `benches/bench-1.12-phase2-replay.ts` (new) — Phase 2 replay
    against 40 target displacements (5 magnitudes × 8 directions,
    100-800 px each axis).
  - `benches/bench-1.12-phase2-debug-signs.ts` (new) — single-emit
    diagnostic that proved v2-wider's signs ARE correct at all
    dt_prev values; the Phase 2 failure isn't a sign-flip bug.

  Phase 2 result: median Euclidean residual **721 px** vs target gate
  **<10 px**. 23/40 cases hit MAX_EMITS=50 without converging.
  Pattern: small targets (+x_100, +x_200, -y_100, etc.) converge in
  <20 emits; large targets (anything with +y, anything ≥400 px)
  diverge.

  Root cause (investigated, not papered over): **multi-step error
  compounding**. v2-wider was trained for single-step 50 ms horizon
  prediction with per-step MAE ~3 px on burst, ~10 px on chunkedBurst-y
  (offline numbers from 1.10). When the planner queries it sequentially
  for 50 chunks, the simulated cursor state drifts further from the
  trained distribution at each step, and the per-step errors grow
  non-linearly. The sign-correct single-emit predictions (verified via
  debug-signs probe) do not imply correctness when chained into a
  closed loop.

  Mechanism that hides this in production: live `moveToPixel` takes
  one `predictDisplacement` call per move and uses `Math.abs()` on the
  output (`learnedBallisticsPxPerMickey` at `move-to.ts:153-154`) —
  the prediction is used only as a magnitude estimate, and the sign
  comes from the command vector. So v2-wider's 1.11 live win is real
  but doesn't transfer to the multi-step open-loop role 1.12 wants.

  Implication for the roadmap: 1.12 as designed needs a different
  forward model. Options for unblocking (none cheap; all real work):
  - (i) Train a model with a longer prediction horizon (e.g., predict
    cursor position after a 16-emit chunked-burst, not after 50 ms).
    Trajectory data from 1.9 already has the inputs but would require
    rewriting `build_examples` in the trainer.
  - (ii) Sequence model (RNN/transformer) that propagates internal
    state, so multi-step compounding learns to self-correct.
  - (iii) Drop open-loop ambition; use v2-wider with the existing
    closed-loop correction (1.11 ships 80% HIT — possibly sufficient
    for moveToPixel's "good enough" target).

  **Status: 1.12 paused, marked blocked.** Phase 3 (live iPad test)
  was scheduled to compound the same errors against real cursor
  dynamics — running it would not produce a usable result. Phase 4
  (combined system A/B) is gated on Phase 3. The planner module +
  test infrastructure remain in tree for whichever forward-model
  redesign (i)/(ii) wins.

- [~] **1.13 Diagnose 1.11 remaining 20% failures — initial closure WALKED BACK after visual audit** (2026-06-02):

  **Walk-back 2026-06-02**: a first pass classified all 4 failures as
  non-pointer-accel ("retry divergence" + tap-registration) on the
  basis of the bench's residual numbers alone. User pushed back; on
  visual inspection of the post-click frames the picture is more
  ambiguous than that closure claimed. Honest revised table:

  | trial | final pos (bench) | target | what the screenshot shows | best current diagnosis |
  |---|---|---|---|---|
  | settings/01 NOLAUNCH | (1039, 837) | (1027, 837) | n/a (positioning OK) | tap-registration failure — NOT pointer-accel |
  | books/03 SKIP | (680, 855) | (757, 837) | cursor visibly AT (~685, 855) — detector correct | **real positioning failure — could be pointer-accel** |
  | books/04 SKIP | (640, 1008) | (757, 837) | cursor not clearly visible at reported pos | **unknown — detector hallucination OR cursor pushed off-screen** |
  | appstore/02 SKIP | (1000, 648) | (1027, 702) | cursor visibly ON App Store icon at target | **detector lied — NOT pointer-accel** (cursor was at target but detector reported 60 px residual) |

  What the per-trial visual evidence actually says:
  - **settings/01**: cursor at 12 px (well under gate); tap didn't
    register on the iPad. Mechanism is independent of cursor
    positioning. Unambiguously NOT pointer-accel.
  - **appstore/02**: post-click frame shows the orange-bordered cursor
    sitting ON the App Store icon (target 1027, 702). The bench
    reported residual 60 px and SKIPped four times in a row. The
    cursor was *already at target* on probably every attempt; the
    SKIP gate fired because the detector kept reporting it elsewhere.
    Unambiguously a detector / verification-path issue, NOT
    pointer-accel.
  - **books/03**: cursor visible at ~(685, 855); bench reported
    (680, 855) — detector is correct. Cursor consistently settles
    ~72 px west and ~18 px south of the Books target across all 4
    attempts. v2-wider sets the pxPerMickey ratio used by the
    open-loop emit; a systematic under-prediction would produce
    exactly this "always lands the same amount short" pattern. **This
    is plausibly pointer-accel-related and was wrongly closed as
    "retry divergence" in the first pass.**
  - **books/04**: cursor not clearly visible at the reported
    (640, 1008) bottom-left position. Could be (a) detector
    hallucination on a cursor-absent frame, (b) v2-wider over-shoot
    pushing cursor past the iPad bottom edge where iPadOS clamps it,
    or (c) edge-unstick logic firing in a bad direction during the
    retry preamble. Without per-attempt frames I can't distinguish.

  Revised honest count:
  - 1/4 unambiguously NOT pointer-accel (settings/01 tap-reg)
  - 1/4 unambiguously NOT pointer-accel (appstore/02 detector-lie)
  - 1/4 plausibly IS pointer-accel (books/03 systematic shortfall)
  - 1/4 unknown (books/04 needs per-attempt instrumentation)

  Where the previous closure was wrong:
  - I conflated "residual numbers going up across attempts" with
    "the cursor itself diverged across attempts". Bench residuals
    are detector-reported positions, not ground truth. When the
    detector lies (appstore/02) or hallucinates (possibly books/04),
    rising residual numbers do not imply the cursor moved.
  - The "retry over-shoots" story was a single-trial extrapolation
    (books/04's 38→249→401→207) without visual evidence that the
    cursor itself moved between those numbers.

  Real next step: rerun a small books-only bench with per-attempt
  frame capture (one frame between every attempt, not just the
  final) and visually audit cursor position vs detector residual on
  each step. THAT data would distinguish pointer-accel-shortfall
  from detector-confusion-on-near-target-frames. Stage 1 is **not**
  closed; 1.11's 80% ship stands but the headroom question is
  reopened.

  **1.13c — clean ground-truth A/B on pointer-accel** (2026-06-02).
  Following user push-back: 1.11 measured click success (HIT/SKIP/
  NOLAUNCH), not pointer-movement accuracy directly. The bench
  `benches/bench-1.13c-groundtruth-pointer-accel.ts` bypasses the
  screenshot detector entirely — iPadCollector reports actual cursor
  position over WS, that is the ground truth. For each trial: slam
  to known start → read iPadCollector start → moveToPixel with
  correct:false (pure open-loop, no correction passes) → read
  iPadCollector end → residual = |actual - target|.

  Two arms ran cleanly at N=15 (v1 arm dropped iPad WS connection
  early both attempts; constant + v2-wider gave a clean comparison):

  | arm | median resid | mean | min | max |
  |---|---|---|---|---|
  | constant (1.0 px/mickey) | **99 px** | 89 | 15 | 145 |
  | v2-wider (model) | **354 px** | 370 | 176 | 566 |

  **v2-wider's pure open-loop is 3.5× WORSE than the constant
  baseline.** The model over-predicts pxPerMickey at cold-start
  (queried with dt=0, no history, zero velocity, chunkMag=20 → it
  returns ~56 logical px, implying pxPerMickey ≈ 2.5 px_hdmi/mickey
  vs actual ~0.8-1.0). moveToPixel divides target_distance by that
  inflated ratio, emits too few mickeys, cursor undershoots.

  This contradicts the "v2-wider improves pointer-movement
  accuracy" claim I made when 1.11 shipped. The +55pp 1.11 HIT lift
  is real (the production bench measures real click success), but
  its mechanism is NOT better open-loop landing. It must come from
  how v2's (poor but consistent) open-loop landing interacts with
  the correction loop — possibly v2's predictable under-emit gives
  the closed-loop iteration a stable starting position, while v1's
  predictions produced larger errors in unpredictable directions
  that the correction loop couldn't converge from. **The honest
  attribution for the +55pp lift is "v2 plays better with the
  correction loop than v1 did", not "v2 lands the cursor more
  accurately on its own".**

  Walk-back of earlier wording: previous roadmap entries described
  v2-wider as "doing its job as a forward-model magnitude
  estimator". That is not what the ground-truth bench shows. The
  estimator is materially worse than the constant. v2-wider should
  stay shipped as default because it lifts the live click rate, but
  the why-it-works story documented elsewhere needs an honest
  rewrite to match this evidence.

  **v1 arm completed after adding sanity cap + reconnect** (2026-06-02
  re-run): the original v1 bench dropped the iPad WS because v1's
  cold-start prediction is so broken on the Y axis (0.011 px_hdmi/
  mickey vs the ~1.0 actual) that moveToPixel emitted 20-65k mickeys
  per trial, clamped the cursor at the iPad's bottom edge, and the
  iPadCollector view died from the input flood. Added a 2000-mickey
  pre-flight sanity check to the bench (`previewLearnedRatio` +
  `SANE_MICKEYS_CAP`): if the model's prediction would drive an
  unreasonable number of mickeys, skip the trial and record the
  ratio. With the cap in place, v1 ran cleanly:

  | arm | measured | skipped | median | mean | min | max |
  |---|---|---|---|---|---|---|
  | constant | 15/15 | 0 | 99 px | 89 | 15 | 145 |
  | v2-wider | 15/15 | 0 | 354 px | 370 | 176 | 566 |
  | v1 | **0/15** | **15** | — | — | — | — |

  v1's cold-start ratio was (x=2.444, y=0.011) px_hdmi/mickey on
  every trial. Even small Y displacements (200-700 px) needed
  20-65k mickeys to plan, far above the sanity cap. This matches
  the 1.6 v1 failure memory ("v1 cold-start predictions are garbage
  across all axes: +y 20 → +0.6 logical px") — production has no
  such cap, which is why 1.6's v1 live A/B catastrophically failed.

  Final picture across all three arms:
  - **constant (no model)**: median 99 px open-loop landing error.
    Limited by iPad's actual pointer-acceleration variance.
  - **v2-wider (shipped)**: 3.5× worse open-loop landing than constant.
    Over-predicts pxPerMickey → under-emits → undershoots ~350 px.
    The 1.11 +55pp HIT lift is real but comes from interaction with
    the correction loop, not better open-loop accuracy.
  - **v1 (deprecated)**: completely unusable open-loop. Cold-start
    Y prediction is off by ~100×. Production has no sanity cap, so
    v1 emits cause WS-killing input floods. This is exactly the 1.6
    failure mode.

  **1.14 — production A/B constant vs v2-wider (both with correction
  loop)** (2026-06-02). The 1.11 baseline was v1-enabled (25% HIT),
  not constant. This bench fills the missing comparison: does v2-wider
  beat the no-model path on the FULL production stack (correction
  loop + retry + safety gate)?

  Same `benches/bench-click-production.ts` as 1.11, 5 trials × 4 targets
  per arm. Both arms run identical retry/correction/gate logic; only the
  open-loop planning ratio differs.

  | arm | HIT | SKIP | MISS | NOLAUNCH |
  |---|---|---|---|---|
  | constant (`PIKVM_USE_LEARNED_BALLISTICS=0`) | **18/20 (90%)** | 1 | 0 | 1 |
  | v2-wider (`PIKVM_USE_LEARNED_BALLISTICS=1` + v2-wider.onnx) | **18/20 (90%)** | 1 | 1 | 0 |

  Per-target tied across all four (Settings 5/5/5/5, Books 4/4, AppStore
  5/5, Files 4/4). At N=20 (±15pp noise) **v2-wider produces no
  detectable click-rate improvement over running with no model at all.**

  Final crystallised story for the whole pointer-accel arc:
  - The 1.11 +55pp lift was real but mis-attributed. It came from
    **removing v1's broken cold-start predictions** (which 1.13c
    showed are catastrophic), not from **adding v2-wider's
    predictions**. v2-wider just happened to be the replacement.
  - The correction loop does the work. The open-loop ratio (constant,
    v2, or otherwise) just seeds it.
  - v2-wider's open-loop is materially WORSE than constant (1.13c:
    354 px vs 99 px median residual). The correction loop
    compensates so end-to-end results match.
  - v1's open-loop fails so catastrophically (60k mickeys → input
    flood → killed iPad app) that the correction loop never gets a
    chance.

  Implications:
  - **Default production path** (`PIKVM_USE_LEARNED_BALLISTICS` unset →
    constant 1.0 px/mickey + correction loop) is already at 90% HIT
    today; this matches the v2-wider "shipped default" path within
    bench noise.
  - The `resolveDefaultModelPath` change (1.11 commit `07aaa95`,
    "v2-wider shipped as default") doesn't measurably help; it only
    activates when the env var is explicitly set, and even then
    gives no detectable lift. Leaving it as-is is harmless (v2-wider
    is the best available model if someone DOES turn the flag on),
    but the README / docs should not recommend turning the flag on
    as a way to improve click rate — the data says it doesn't.
  - The originally claimed 93% (v11 memory) / 80% (1.11 measured)
    band is consistent with today's 90% — all of these are inside
    the ±10-15pp bench noise.
  - **Stage 1 closes for real this time.** Three honest results:
    (i) v1 cold-start broken, never shipped (1.6, confirmed 1.13c);
    (ii) v2-wider doesn't measurably beat constant (1.14);
    (iii) constant + correction loop + retry is at ~90% HIT today,
    the same 1.11 said.

  **1.13b — per-attempt bench executed 2026-06-02:** `benches/bench-
  1.13-books-perattempt.ts` runs N trials × 4 attempts on Books,
  bypassing clickAtWithRetry's retry loop and saving a fresh frame
  after each `moveToPixel` call (output: `data/bench-1.13-books-
  perattempt/`). Ran with N=8 (32 attempts total, no clicks fired).

  Detector-reported per-attempt residuals (px):
  ```
  trial 1:  598 → 206 → 105 →  85   (converging open-loop, normal)
  trial 2: null → 424 → 207 →  22   (finally lands at-target)
  trial 3:   85 →   3 → 469 →  92   (a2 AT target, a3 jumps far)
  trial 4:   19 → null → 271 →  42   (a1 AT target, a3 jumps)
  trial 5:  143 →  19 →  57 →  85   (a2 at target, drift)
  trial 6:    7 →  46 → 159 →  97   (a1 at target, drift)
  trial 7: null →  60 →  13 → 424   (a3 AT target, a4 jumps)
  trial 8:  207 →  46 →   9 →  58   (a3 AT target, drift)
  ```

  Visual audit (the part the bench can't do automatically):
  - **trial 3 a2** (resid 3): cursor visibly ON Books icon — correct.
  - **trial 3 a3** (resid 469, reported pos (1204, 980)): cursor STILL
    visibly on Books icon, basically unchanged from a2. Detector
    hallucinated the 469 px residual.
  - **trial 7 a3** (resid 13): cursor on Books — correct.
  - **trial 7 a4** (resid 424, reported pos (1170, 932)): cursor STILL
    on Books, barely moved. Detector hallucinated again.

  Conclusion: the "retry divergence" pattern (a1 near-target → a2 huge
  residual) is **detector flip-flopping**, not cursor motion. v2-wider
  + moveToPixel reach the Books target in ~50% of attempts (trials
  3/4/5/6/7/8 each have at least one at-target frame); the detector
  intermittently mislocates the cursor on subsequent attempts. The
  Books icon + orange-bordered cursor combination is the likely
  failure mode (orange-on-orange low contrast).

  **Revised final answer to "is the remaining 20% pointer-accel?":**
  NO. v2-wider is doing its job. The 1.11 SKIPs at Books and AppStore
  are dominated by the v9-bordered detector flip-flopping when cursor
  sits on an icon. settings/01 is a separate tap-registration issue.
  The headroom from 80% → 95% live HIT is **detector robustness
  work** — specifically: get the cursor-on-icon detection reliable
  enough that the safety gate doesn't fire on phantom residuals.

  Stage 1 (pointer-accel) is **now closed** at 1.11's 80% ship; the
  bottleneck has been correctly attributed and lies outside this
  stage. Future work routes to a detector-confidence stage.

## Stage 2 — v12.1 detector with balanced absent frames

Current v12 is trained on 11030 visible + 56 absent (0.5 %). With no absent-cursor signal it confidently picks *something* when the pointer has actually faded — measurable failure mode.

- [x] **2.1 Add `--absent-fraction` to `benches/bench-collect-synthetic.ts`** — Verified live 2026-05-31. 50-frame run with `--absent-fraction 0.5` produced 31 absent + 19 visible rows (output `data/cursor-collect-synthetic-2026-05-31T05-32-13/`). Spot-checked frame-00001.jpg: no cursor anywhere on the iPad screen, 12 s wait does its job. Schema matches `bench-collect-absent.ts:74`. **Bonus fix shipped same session** (`c5ef0a1`): the PA39 text-field overlay needed a pointer-move nudge after `setOverlay` for iPadOS to morph the cursor sprite — verified live, 4/4 i-beam-labeled frames now show the I-beam sprite. The earlier 30-frame run `data/cursor-collect-synthetic-2026-05-31T05-12-08/` was deleted (it had arrow cursors mislabeled as i-beam).
- [x] **2.2 Collect 2000-frame absent-balanced train + 1000-frame absent-balanced val** — Verified 2026-05-31. Train run `cursor-collect-synthetic-2026-05-31T14-20-38`: `--target 2000 --absent-fraction 0.5` → **1896 saved** (104 skipped pointer-not-awake, 5.2 %), 902 visible / 994 absent (48/52), 12605 s = 3.5 h. Val run `cursor-collect-synthetic-2026-05-31T18-24-40`: same args + `--exclude-list train-image-scenes.txt` (1485 unique image scenes from train), → **959 saved** (41 skipped, 4.3 %), 462 visible / 497 absent (48/52), 6303 s = 1.75 h. Total 2855 labeled frames, zero scene overlap by `--exclude-list` discipline. One mid-run val restart happened (~3.5 h of original val collection lost, then full restart took the 1.75 h figure above — root cause likely a cron-fire that interpreted in-flight bench as orphaned and killed it; future cron prompts should distinguish "stuck" from "session-active").
- [x] **2.3 Train v12.1** — Verified 2026-06-01. Forked to `ml/train-cursor-v12.1.py`; added the 2026-05-31 train collection (1896 frames) to SOURCES, replaced EXTERNAL_VAL_SOURCES with the new val (959 frames). Two trainer fixes shipped this fire: (a) **`pa26` source dropped** — its frames live under `data/click-bench-prod/` which today's bench-click-production runs (3.5 + N=20 + Books re-aim) `rm -rf`'d and rewrote with different per-trial verdicts, so the 60 pa26 frame paths were stale; re-locating them is not worth it when 2855 fresh frames already swamp the corpus. (b) **`cursor: null` → `{"visible": False}` normalization** at load time — bench-collect-synthetic writes literal `cursor: null` for absent frames; the old loader skipped them, dropping 994 absent rows from the new train set and 497 from the new val set (silently killing the absent-balanced training). After fix: **train 12866 (11816 pos / 1050 neg) | val 959 (462 pos / 497 neg)** [hard image-catalog split, zero scene overlap].
  Trained 40 epochs in ~45 min on MPS. Best epoch 29-38: `med_dist=0.0px(input), det=99.6%, fp=1.21%, combined=1.2`. **Caveat:** med_dist is quantized to heatmap-cell resolution (1 cell = 4 input-px); 0.0 means median predicted peak in the *exact same cell* as the target. Not the MPS-CPU silent-bug (verified: both operand tensors on DEVICE per the comment fix; transition 4.0→0.0 at epoch 20 then settling 25+ is normal cell-snap behavior). **The val is synthetic, NOT iPad UI**; these numbers say nothing about real click-rate. 2.4 live A/B against iPad is the ship gate.
  Output: `ml/cursor-v12.1.pt` + `ml/cursor-v12.1-val-manifest.jsonl`. Full log: `ml/train-v12.1.log`.

  **1.10 — four v2 training attempts; none clear the chunkedBurst gate** (2026-06-02):
  | attempt | hidden | layers | history_ms | linearity | direction | burst | chunkedBurst | randomWalk |
  |---|---|---|---|---|---|---|---|---|
  | original v2 | 32 | 3 | 100 | 1.29 / 2.86 | 2.57 / 3.43 | 6.65 / 1.64 | **7.15 / 11.65** | 8.34 / 7.62 |
  | wider v2 | 64 | 4 | 100 | 1.17 / 1.19 | 1.21 / 2.11 | 6.44 / 0.77 | **6.40 / 10.37** | 9.95 / 7.42 |
  | wider + HW500 | 64 | 4 | 500 | 2.09 / 1.28 | 3.74 / 3.10 | 6.71 / 1.71 | **7.06 / 14.71** | 9.21 / 8.68 |
  | wider + last_emit | 64 | 4 | 100 | 1.52 / 2.34 | 1.87 / 3.49 | 6.57 / 2.06 | **5.28 / 12.19** | 9.01 / 8.28 |

  Wider helped the old families (linearity, direction, burst y) reach ~v1 quality. Longer history window made things worse (plain cumulative sum over 500 ms adds inter-chain noise without temporal structure). Adding `last_emit_dx/dy` features dropped chunkedBurst x slightly (6.40 → 5.28) at the cost of y regression (10.37 → 12.19) AND mild degradation across all old families' y axes — net worse. **chunkedBurst y MAE stuck at 10-14 px across all four attempts** — appears structurally bounded by summary-feature representations at this horizon. Most likely interpretation: the 50 ms horizon target for chunkedBurst-y is intrinsically noisy (iPad pointer-acceleration variance is high in that regime); no summary-feature model can fit it. To break this would need (i) a sequence model that sees raw last-N emits (RNN/transformer, ~half day of work), or (ii) accept that chunked-burst regime isn't predictable at this horizon and only use the model for the regimes it does fit (linearity, burst, direction). **Iteration paused — needs user direction on whether to invest in (i) or accept (ii).**

  Artifacts: `ml/pointer-accel-v2.pt` (original), `ml/pointer-accel-v2-wider.pt` (best old-family fit, 64×4 HW=100), `ml/pointer-accel-v2-hw500.pt` (regression), `ml/pointer-accel-v2-lastemit.pt` (10 features).
- [x] **2.4 Export ONNX + live A/B v12 vs v12.1** — Verified 2026-06-01. New `ml/export-v12.1-onnx.py`; exported `ml/cursor-v12.1.onnx` (8.7 MB). Live N=5 × 4-target bench against iPad on page 1:
  - **v12 (baseline)**: Settings 5/5, Books 4/5, AppStore 2/5, Files 5/5 → **16/20 = 80 % HIT**. 3 NO_LAUNCH (AppStore × 3), 1 SKIP (Books). Saved `data/click-bench-prod-v12-A/`.
  - **v12.1 (treatment, `PIKVM_ML_V8_MODEL=ml/cursor-v12.1.onnx`)**: Settings 4/5, Books 3/5, AppStore 3/5, Files 5/5 → **15/20 = 75 % HIT**. 4 NO_LAUNCH (Settings, Books, AppStore × 2), 1 SKIP (Books). Saved `data/click-bench-prod-v12.1-B/`.
  - Acceptance: fp on synth val 1.21 % < 10 % ✓; HIT 75 % ≥ v12 − 5 pp = 75 % ✓ (at threshold). v12.1 **passes by 0 pp**.
- [x] **2.5 Promote v12.1 default if it passes** — **DECISION: do not promote**. 2026-06-01. v12.1 passes the acceptance criterion by exactly 0 pp. At N=20 per arm the 95 % CI is ±15 pp; the 5-pp gap between 80 % and 75 % is well inside noise. Earlier today the same v12 measured 85 % at N=80, so v12 itself moves ~±10 pp run-to-run. There is **no measurable click-rate improvement** from the 2855-frame data expansion. Consistent with prior negative results (PA31 v10→v9, V3b v3→v1) — bigger training corpora don't translate into iPad click-rate at this measurement resolution. `cursor-ml-detect.ts` candidate order unchanged; v12 remains default. v12.1 artefacts kept on disk for future re-evaluation if the bench's effective noise floor drops (e.g. via N≥80 paired-difference design).

## Stage 3 — iPadOS tap-registration investigation

On today's iPad, `Settings` and `Books` are 0/5 for both v11 *and* v12. The taps land at the right pixel but iPadOS doesn't fire `app launch`. Detection is *not* the bottleneck here.

- [x] **3.1 Reproduce in isolation** — Verified 2026-05-31. `benches/bench-tap-isolation.ts` (new). First run with hardcoded `(1027, 837)` (Settings on **page 1**) confirmed the screen reference showed iPad **home page 2** — those coords land on empty wallpaper. Re-ran against iPadCollector at `(1067, 700)` (a confirmed page-2 icon): **7/10 HIT** (3 NO_LAUNCH all had residual ≥ 90 px = cursor missed icon). Manual frame check: HIT post-frames really show iPadCollector launched. **Tap registration is not the bottleneck.** Today's 85 % NO_LAUNCH was the bench targeting page-1 coords against a page-2 iPad. Full diagnostic: `docs/troubleshooting/2026-05-31-3.1-tap-registration-fine.md`.
- [~] **3.2 Click-duration sweep / 3.3 Inter-click delay / 3.4 Snap-zone offset** — likely unneeded. 3.1's data shows iPadOS launches reliably when cursor is on icon (residual ≤ 31 px → 100 % HIT; residual ~90 px → still 50 % HIT). No tap-rejection pattern to chase. Defer; revisit only if a future bench-with-correct-coords still shows < 80 % HIT on icon-sized targets.
- [x] **3.5 Fix the production bench to verify target coords are real icons on the current screen** — 2026-05-31. Implemented as a page-1 *image-similarity* sanity gate rather than per-icon template matching (simpler, no per-target template upkeep). Added `rgbFromJpegCroppedToIpad` + `similarityCroppedToIpad` to `benches/bench-click-production.ts`; the gate crops both the freshly-captured home screenshot and `benches/fixtures/page-1-home-reference.jpg` to the detected iPad-tight region before resizing to 96×54 RGB, then mean-abs-diff. The earlier uncropped 96×54 included the ~50 % black-bezel pixels in every frame, so page-1 vs page-2 still scored ~0.97 (above the original 0.92 threshold) — gate would have silently passed page-2. Cropping shifts page-1-vs-page-2 to ~0.906; page-1 self-similarity stays at 1.0. New threshold = **0.94** with ~0.04 margin on each side. Verified live 2026-05-31:
  - iPad on page 2 (today's state): bench refused to start at `sim=0.906 < 0.94` with explicit "swipe to page 1 / update TARGETS" guidance, saved `current-home-vs-page1-mismatch.jpg` for inspection.
  - 3 historical page-1 captures (from 2026-05-30 v11 benches, different day / different clock-widget time): `sim={0.9807, 0.9821, 0.9807}` → all PASS comfortably.
  This blocks future wrong-page artifacts at the source — without it today's 0 %/5 %/15 % numbers across PA37, 1.6 baseline, 1.6 treatment would have recurred any time the iPad happened to be on page 2.

## Stage 4 — Scale validation (Phase B/C per original plan)

- [!] **4.0 Reframe Stage 4 in light of 2.5 + 1.13b findings** (2026-06-02). Stage 4 was queued before two later results invalidated its premise:

  1. **2.5** (closed 2026-06-01): a 2855-frame data expansion produced no measurable click-rate lift over v12 (80% → 75% at N=20, well inside the ±15pp noise band). The 2.5 conclusion was explicit: "no measurable click-rate improvement from the 2855-frame data expansion. Consistent with prior negative results (PA31 v10→v9, V3b v3→v1) — bigger training corpora don't translate into iPad click-rate at this measurement resolution."
  2. **1.13b** (closed 2026-06-02): per-attempt frame audit on Books showed the 1.11 80%-ceiling bottleneck is the v9-bordered detector **flip-flopping** on cursor-on-Books-icon frames — visually confirmed cursor on icon while detector reports 200-450 px residual. Specific failure mode: orange-bordered cursor on orange Books icon = low contrast.

  Implication: 4.1's bet (10× generic-distribution data → better detection) is exactly the "bigger corpus" approach 2.5 rules out at this bench resolution, and it doesn't address 1.13b's specific failure class. Running 4.1 as written would burn ~2 h of iPad time + several hours of training on a predicted-no-measurable-lift experiment.

  Stage 4 redirected. Old 4.1/4.2/4.3 below marked as superseded; new targeted steps replace them.

- [~] ~~**4.1 20k-frame overnight synth run**~~ — **SUPERSEDED 2026-06-02 by 4.0.** Generic-distribution scale-up doesn't move the needle at this bench's noise floor (2.5 measured). Original spec: `npx tsx benches/bench-collect-synthetic.ts --target 20000`, ~110 min at 3 fps.
- [~] ~~**4.2 Train cursor-v13 on 20k + prior**~~ — **SUPERSEDED 2026-06-02 by 4.0.** Gated on the superseded 4.1.
- [~] ~~**4.3 Live A/B v13 vs current default**~~ — **SUPERSEDED 2026-06-02 by 4.0.** Gated on the superseded 4.2.

- [~] **4.1' Targeted Books-icon corpus** — replace 4.1 with a **failure-mode-targeted** collection: ~500-1000 frames of cursor sitting on the Books icon (and other iPad-default orange icons: TV, Books-folder, App Store) at the small (~5-20 px) intra-icon offsets where 1.13b showed the detector hallucinating. Likely path: extend `bench-collect-synthetic.ts` with a `--scene cursor-on-icon --icon books` mode that drives cursor to small offsets around the icon's center and records human-verified labels via the iPadCollector ground truth.

  **4.1'-scoping (2026-06-02)**: ran the existing per-attempt Books bench at N=10 (40 attempts total). Pattern matches 1.13b — residuals jump wildly across attempts. Visual spot-check of two suspect frames revealed the picture is more nuanced than 1.13b's "all high residuals are hallucinations" closure:
  - **trial 7 a4** (residual 113 px at reported pos (696, 932)): cursor visibly ON Books icon at (~756, 838). This IS detector hallucination — reproduces 1.13b's failure mode.
  - **trial 9 a4** (residual 175 px at reported pos (720, 1008)): cursor visibly AT the bottom edge of the screen near (737, 1023). The detector is approximately correct. The high residual is **real cursor displacement** — the retry loop's detect probe + open-loop emit drove the cursor 190 px away from the Books target between a3 (resid 3, cursor on icon) and a4.

  Two independent categories of high-residual frame:
  - (i) **Detector hallucination**: cursor on icon, detector reports far away. Justifies 4.1' detector corpus retrain.
  - (ii) **Retry instability**: cursor genuinely moves away between attempts, sometimes catastrophically. NOT a detector issue — it's the retry loop's detect-probe + correction emits doing the wrong thing. Different work, different stage.

  Honest pre-condition reframed: **before collecting a 500-1000 frame detector corpus, find out what fraction of the production failures are (i) vs (ii)**. A detector corpus only fixes (i); (ii) would need retry-strategy work in `move-to.ts` / `click-verify.ts`. With production at 90% HIT today (1.14), this matters less — but if the user wants to push toward 95%+, the split between (i) and (ii) decides where to spend time. Suggested next sub-step: visually audit all 40 frames from today's bench (~30 min), classify each high-residual frame as (i)/(ii)/cursor-correctly-far, get a ratio. The ratio is the input to the corpus-vs-retry decision.

  **Audit complete (2026-06-02)** — all 40 frames classified by a sub-agent reading each JPG. Tally:

  | category | count | % |
  |---|---|---|
  | HIT (cursor on Books, detector correct) | 11 | 27.5% |
  | (i) HALLUCINATION (cursor on icon, detector lies) | 5 | 12.5% |
  | (ii) REAL-MOVE-AWAY (cursor genuinely elsewhere) | 21 | 52.5% |
  | NULL (no detector report; 3/3 had cursor on/near Books) | 3 | 7.5% |

  **Ratio (i):(ii) = 5:21 ≈ 1:4. REAL-MOVE-AWAY dominates 4× over HALLUCINATION.**

  Where the cursor ended up on the 21 REAL-MOVE-AWAY frames: dock icons (Music, Firefox, Messages, ~5 cases), below Books label (~8 cases), left/right of Books in adjacent columns (~5 cases), bottom edge of screen (~3 cases). The retry loop's detect-probe + open-loop emit is systematically driving cursor into the row below Books and into the dock.

  Bonus finding: 1 "inverse hallucination" (trial 3 a4) — cursor visibly below Books at ~(750, 910), detector falsely reported on-target with residual 24. The detector can also report false-positive at-target, which is strictly worse than missing (it would let a SKIP-worthy click proceed onto an adjacent UI element).

  Decision: **4.1' detector corpus is worth doing but won't move the needle as much as initially hoped.** It addresses ~12-20% of frames (5 hallucinations + 3 NULLs). The bigger lever is **investigating why the retry loop's probe + emit drives cursor 50-200 px away from a previously-on-target Books position** — this is `move-to.ts` / `click-verify.ts` work, NOT detector work. A new Stage 5 (retry instability) is the higher-leverage next direction.

- [~] **5.1 Retry-instability fix — skip detect probe when previous attempt landed at-target** (2026-06-02). Hypothesis from audit: each retry's moveToPixel runs a detect-then-move probe that emits 60 mickeys, and that probe motion + ratio-variance overshoot is what drives cursor 50-200 px AWAY from a previously on-target position. Fix: when the previous attempt's `lastMoveResult.finalDetectedPosition` is within `maxResidualPx` of target, this attempt uses `strategy: 'assume-at'` with the previous detected position, bypassing the destabilizing probe. Surgical conditional in `click-verify.ts:650-677`; falls back to current behavior when previous attempt was off-target.

  Live verification (5×4 production bench, `PIKVM_USE_LEARNED_BALLISTICS=0`):

  | metric | 1.14 baseline (constant) | 5.1 fix |
  |---|---|---|
  | HIT | 18/20 (90%) | **18/20 (90%)** |
  | SKIP | 1/20 (5%) | **0/20 (0%)** |
  | MISS | 0/20 (0%) | 0/20 (0%) |
  | NOLAUNCH | 1/20 (5%) | 2/20 (10%) |
  | per-target Books | 4/5 | **5/5** |

  No measurable change in the overall HIT rate (90% → 90%, well inside ±15pp at N=20). What did change:
  - SKIPs dropped to zero — the failure mode of "retry loop never got cursor within 35 px" is gone. Books specifically went 4/5 → 5/5.
  - 2 trials migrated from HIT-eligible into NOLAUNCH. Cursor reached target, click fired, iPad didn't launch the app. This is the tap-registration failure mode (settings/01 in 1.11) — orthogonal to positioning.

  Net read: the fix is doing what the audit predicted (positioning to target reaches more often, SKIPs eliminated), but the gross click rate is gated downstream by tap-registration reliability that this stage doesn't touch. At N=20 we can't distinguish "no real lift" from "a few-pp lift hidden in the noise". Kept shipped because:
  - The change is principled (tied to a measured failure mode).
  - Eliminates SKIPs as a user-visible failure.
  - Doesn't make any metric worse beyond noise.
  - Future N≥80 stress test could surface a clearer signal.

  Test pass note: 13/15 click-retry tests pass; the 2 failing tests are pre-existing (Phase 36 + Phase 72) and verified untouched by this change via `git stash` baseline comparison.

- [x] **6.0 Ground-truth click-flow bench — every prior HIT number was overstated by ~45pp** (2026-06-02). User-requested. The production bench's HIT classifier uses screenshot diff + home-similarity; it has no way to know whether the tap actually landed on the icon. iPadCollector's `onTapEvent` reports the actual tap location in iPad logical px, which converts back to HDMI px via the iPad-region transform. `benches/bench-ground-truth-clickflow.ts` runs `clickAtWithRetry` against an iPadCollector-rendered home-screen image and collects every tap event during retries; HIT = at least one tap landed within 35 HDMI px of target.

  N=5 per target × 4 targets per arm:

  | arm | ground-truth HIT | screenshot-bench HIT (1.14, yesterday) |
  |---|---|---|
  | constant (no model) | **9/20 = 45%** | 18/20 = 90% |
  | v2-wider (current default) | **8/20 = 40%** | 18/20 = 90% |

  Per-target ground-truth:

  | target | constant | v2-wider |
  |---|---|---|
  | Settings | 3/5 | 3/5 |
  | Books | 2/5 | 1/5 |
  | AppStore | **1/5** | **0/5** |
  | Files | 3/5 | 4/5 |

  Implications, in plain English:
  1. The production click bench has been systematically reporting +45pp more HITs than the tap actually landed on the icon. Every prior +/-Npp claim measured with that bench (1.11, 1.14, PA25, PA34-37, R4, V3b, the v9/v10/v11/v12 detector A/Bs, etc.) carries the same bias and needs re-evaluation.
  2. The 1.11 "+55pp pointer-accel lift" was almost certainly a screenshot-classification artifact, not a real positioning lift. v1's catastrophic emits produced post-click frames the screen-similarity check called "no change" (no app launch). v2's emits produced frames just-different-enough to be classified as launches. Underlying tap-landing rates between the two arms are statistically indistinguishable at this N.
  3. AppStore is functionally broken — 1/5 with no model, 0/5 with v2-wider. Files is the best target (3-4/5). Books is in the middle. There is real per-target structure here that the screenshot bench was averaging away.
  4. Honest production HIT rate today: **40-45%**, not 90%. The README's 85-93% range is the screenshot-bench overstating reality; the true number lives in the ground-truth bench.

  Next, per user direction: re-run pointer-accel A/Bs against this ground-truth bench before drawing any more conclusions about the ML model contributions. Queue (rough order):
  - [x] **6.1 / 6.2 combined: five-arm pointer-movement A/B** (2026-06-02) — exported the missing v2 and v2-hw500 ONNX files; ran `bench-1.13c-groundtruth-pointer-accel.ts` against all available variants. Pure-open-loop pointer-movement landing residuals (N=15 per arm):

    | arm | measured | median | mean | p95 | max |
    |---|---|---|---|---|---|
    | constant (no model, 1.0 px/mickey) | 15/15 | **99 px** | 87 | 145 | 145 |
    | v2-original (32×3, 8 feat) | 15/15 | 170 px | 172 | 271 | 271 |
    | v2-hw500 (64×4, HW=500) | 15/15 | 176 px | 178 | 275 | 275 |
    | v2-wider (64×4, HW=100, shipped default) | 15/15 | **354 px** | 372 | 566 | 566 |
    | v1 (catastrophic cold-start) | 0/15 | — | — | — | — (all 15 skipped by 2000-mickey sanity cap) |

    Conclusions, in plain English:
    1. **No trained variant beats constant 1.0 in pure open-loop.** Every single one is worse by 70-355 px median. The whole pointer-accel modeling exercise produced a measurably-worse forward predictor than no model at every variant.
    2. **v2-wider, the currently shipped default, is the WORST runnable variant** — 3.5× worse than constant, ~2× worse than v2-original. The 1.10 decision to drop v2-original → v2-wider was based on offline chunkedBurst-y MAE, which my ground-truth measurement here directly contradicts in the regime production actually uses.
    3. **v1 cold-start predictions remain catastrophic.** All 15 trials skipped by the sanity cap (the same failure mode as 1.6).
    4. The shipped `resolveDefaultModelPath` should be reverted to v1.onnx — except v1 is also broken, so the correct production setting is `PIKVM_USE_LEARNED_BALLISTICS=0` (the env-unset default already disables the model). The model's existence is dead code path under default config; the README should not recommend turning the flag on, and the code path should probably be removed unless someone has a use case the ground-truth bench hasn't measured.
    5. Reconciling with 6.0: even though v2-wider is much worse on pure pointer-movement, its 6.0 ground-truth HIT rate (40%) was statistically indistinguishable from constant's (45%). The correction loop fully covers for the model's mistakes; gross click rate is gated by something downstream (likely click-verify retry quirks + tap reliability), not by which model variant seeds the loop.
    6. v2-lastemit (10-feature variant) deferred — requires a runtime FEATURE_DIM change before it can be loaded; not worth the code change given (1) above.
  - **6.3 1.13c re-run at N≥30** — the 15-trial pointer-movement bench (constant 99 px median vs v2-wider 354 px) carries "v2 is worse open-loop"; deserves tighter confidence intervals.
  - **6.4 v2-wider prediction-vs-reality regime map** — sweep (chunkMag, paceMs, dt_prev, prior velocity), emit one chunk, measure cursor displacement via iPadCollector, compare to v2-wider's prediction. Produces a map of "where does the model match reality"; informs whether further model work is worth doing.
  - **6.5 1.12 Phase 3 open-loop planner against iPad** — was queued, skipped after Phase 2 failed on synthetic data. Now runnable with iPadCollector ground truth.
  - **6.6 5.1 fix paired A/B** — add 5.1-fix-on vs 5.1-fix-off as a second arm axis on the ground-truth bench. Today's measurement was single-arm.
- [ ] **4.2' Train cursor-v13 on targeted-corpus + prior** — same trainer as v12.1 but with the new on-icon frames added. Evaluate the resulting model SPECIFICALLY on cursor-on-icon frames (not just held-out aggregate) — that's the metric 1.13b says matters.
- [ ] **4.3' Live A/B v13 vs v12 on Books target with per-attempt frame capture** — reuse `benches/bench-1.13-books-perattempt.ts` pattern. Pass criterion: detector flip-flop rate on cursor-on-icon frames drops measurably. **N≥20 trials per arm** (matches 1.13b sample) AND visual audit of every divergence frame (matches what made 1.13b's audit conclusive). Live HIT-rate gate stays the secondary metric since 2.5 says that's noisy at this N.

## Stage 5 — Cleanup + commit hygiene

- [x] **5.1 Move stale scratch scripts older than 7 days** to `scratch/_archive/`. Don't delete. 2026-05-31: scanned `scratch/` (148 files). Only 2 qualified by mtime + 7 days: `test-phase310-inspect-cluster.ts.bak` + `test-phase312-crop.ts.bak`. Moved both to `scratch/_archive/`. The other 146 files all have mtime within the last week (active scratch from current iteration); no change.
- [x] **5.2 Update `README.md` final-state section** with the actual achievable click rate post-Stage 3, not the old 93 % aspirational number. 2026-06-01. Three targeted edits: (a) "§ iPad cursor configuration" headline now cites **85 % HIT at N=80 (2026-05-31)** with explicit ±10 pp run-to-run band, framing both the older 90 % (N=20) and 93 % (v11) figures as inside that band — no detector-level lift across v9/v11/v12/v12.1 exceeds the noise floor at this resolution. (b) Bundled-model paragraph updated `cursor-v9-bordered.onnx (700 frames)` → `cursor-v12.onnx (~11 k frames = 1030 human-verified + 10 k synthetic)`, with documented fall-back chain v12 → v11 → v9-bordered → v8 matching `cursor-ml-detect.ts` candidate order. (c) Reliability-matrix paragraph's "90 % hit, 0 % miss on 4×5" sentence now followed by the 2026-05-31 N=80 re-measure noting both numbers sit in the same band.
- [x] **5.3 Ensure all in-flight changes are committed** before each Stage transition. Don't leave dangling work in the tree across cron fires. 2026-06-01. Two flush commits:
  - `6632b03` — brightness rename ("screen too dim" → "iPad display blocked", with `ipadDisplayBlocked` field) + matching `src/index.ts` precheck error + test regex + `flake.nix` `nix run .#label-review` app + refreshed `npmDepsHash`. Sat modified across the entire session; coherent + self-contained.
  - `6e91f24`-ish — `ipad-region-detect.test.ts` (pins the 3.5 transform contract) + `scripts/diag-move-to-on-synth.ts` (the diagnostic written during the move-to investigation earlier in this session).
  - Intentionally NOT committed: historical ml/cursor-v6..v10 checkpoints (~90 MB), historical train/export scripts for those versions, `verify-browser.ts` / `verify-zoom-crop.ts` in tools/label-review (per `feedback_destructive_verify_browser` memory; leaving untracked rather than risk dual-use exposure), Xcode `.xcuserstate` (per-user IDE state — should be gitignored; separate hygiene change for another fire), and the workstream output dirs `docs/ml-architecture-bench/` + `docs/troubleshooting/example-frames/`.

---

## Cross-cutting rules

- **No verdicts from samples.** If a bench shows 0 %, the next action is the next experiment, not a "root cause" doc from 3 of 27 frames. Sample all data or label conclusions as hypotheses (see `feedback_no_verdicts_from_samples` memory).
- **Screenshots are source of truth.** Always visually inspect intermediate frames before concluding a model is working (`feedback_screenshots_source_of_truth`).
- **Detector residual is not ground truth.** Don't infer "iPad ignored the tap" from detector-reported residuals (`feedback_detector_residual_is_not_ground_truth`).
- **MPS-CPU tensor co-location.** All PyTorch arithmetic ops on MPS must have operands on the same device, or values silently come back wrong (`feedback_mps_cpu_tensor_subtraction_silent_bug`).
- **REJECTED CLAIMS list** in `feedback_rejected_unverified_claims` — don't assert "pointer-effect snap" / "rate-limit dead zone" / "iPad ignores tap" without citing a screenshot, code path, or this-session data.
- **Pair every live A/B with an iPadCollector ground-truth bench.** The live production bench's residuals come from the screenshot detector and can lie. iPadCollector (`ipad-collector/`, port 8767) exposes ground-truth cursor positions and ground-truth tap locations via WS. `showScene({kind: 'image', ...})` accepts arbitrary images — including a PiKVM screenshot of the real home screen — so you get the same visual surface as production PLUS ground truth. Example: `benches/bench-1.13c-groundtruth-pointer-accel.ts`, `benches/bench-5.1-groundtruth-retry-loop.ts`. Skipping this is how the 1.11 "+55pp pointer-accel lift" claim survived 6 fires before 1.13c/1.14 walked it back. Full rationale: `feedback_ipadcollector_ground_truth` memory.

## Cron-fire workflow (read this first when fired)

1. `git pull --rebase --autostash` if there's a remote — there isn't one yet, so just survey current branch.
2. `git status` — clean tree or honest stash before starting.
3. Read this whole doc. Pick the highest-priority unchecked step.
4. Execute it. Live-test where applicable. Save screenshots when human eyes might disagree with metrics.
5. Update this doc: mark step done, record actual results inline.
6. `git add` the work; commit with a message that names the step (e.g. `feat(roadmap): 1.5 wire pointer-accel into move-to`).
7. Stop. Don't try to do the next step in the same fire — let the next cron handle it. (Exception: trivial follow-ups like updating the plan doc.)

If you finish all of Stages 1–4: cancel your own cron with `CronDelete` (the job ID from when you ran `CronList`) and write a final summary in this doc.
