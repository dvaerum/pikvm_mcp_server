[package]
name = "pikvm-mcp-cursor-belief"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Kalman-style cursor position/ratio estimator, faithful port of src/pikvm/cursor-belief.ts. Pure/deterministic/no I/O, zero PiKVM-domain or foundation dependency by design — split into its own crate because module 2 (kvmd-client, PiKVMClient.belief) and module 3 (detection/vision, cursor-locator/cursor-belief's own transition logic) both need the CursorBelief type directly, not just a consumer relationship. See docs/rust-port-plan.md §7."

[dependencies]
